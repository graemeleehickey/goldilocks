% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test.R
\name{test}
\alias{test}
\title{Calculate posterior probability of the alternative hypothesis}
\usage{
test(post_probs, alternative, h0, single_arm)
}
\arguments{
\item{post_probs}{matrix (3 columns). Sampled draws from the posterior
distribution of the event rate for the \code{treatment} arm and the
\code{control} arm. If the trial is single-armed, the \code{control} arm
probabilities will all be \code{NA}s. The third column is the derived
treatment \code{effect}.}

\item{alternative}{character. The string specifying the alternative
hypothesis, must be one of \code{"greater"} (default), \code{"less"} or
\code{"two-sided"}.}

\item{h0}{scalar. Null hypothesis value of \eqn{p_\text{treatment} -
p_\text{control}}. Default is \code{h0 = 0}.}

\item{single_arm}{logical. If \code{TRUE}, trial is single arm. Else, if
\code{FALSE}, it is a randomized two-arm trial.}
}
\value{
A posterior probability (scalar \eqn{[0, 1]}).
}
\description{
For a given null hypothesis value (\code{h0}), the posterior
probability of the alternative hypothesis is calculated. This can be
compared to threshold values for declaring expected success or futility.
}
