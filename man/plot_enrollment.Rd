% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_enrollment.R
\name{plot_enrollment}
\alias{plot_enrollment}
\title{Plot an enrollment projection}
\usage{
plot_enrollment(
  x = NULL,
  lambda = NULL,
  N_total = NULL,
  lambda_time = NULL,
  interim_look = NULL,
  end_of_study = NULL,
  n_sim = 20L,
  seed = NULL,
  time_unit = NULL,
  xlab = NULL,
  ylab = "Cumulative number of enrolled patients",
  main = NULL,
  annotate = TRUE,
  projection_col = "#276E9B",
  simulation_col = "#777777",
  milestone_col = "#C8682A"
)
}
\arguments{
\item{x}{\code{NULL}, or a result returned by \code{\link[=survival_adapt]{survival_adapt()}} or
\code{\link[=sim_trials]{sim_trials()}}. Results created by current versions of \code{goldilocks} retain
the evaluated enrollment design needed by this function.}

\item{lambda}{finite positive enrollment rates per unit time. Required when
\code{x = NULL}; otherwise it can override the rate stored in \code{x}.}

\item{N_total}{positive integer maximum sample size. Required when
\code{x = NULL}; otherwise it can override the value stored in \code{x}.}

\item{lambda_time}{\code{NULL}, or the finite, positive, strictly increasing
internal times at which the enrollment rate changes. See \code{\link[=enrollment]{enrollment()}}.}

\item{interim_look}{\code{NULL}, or the enrollment counts at interim looks.}

\item{end_of_study}{optional positive follow-up duration. When available and
\code{annotate = TRUE}, it is reported beneath the plot.}

\item{n_sim}{non-negative integer number of random enrollment trajectories
to draw.}

\item{seed}{\code{NULL}, or a single non-negative integer seed for the random
trajectories. A supplied seed does not alter the caller's random-number
state.}

\item{time_unit}{\code{NULL}, or a character label for the design's unit of time,
such as \code{"months"} or \code{"days"}.}

\item{xlab, ylab, main}{axis and main-title labels. With \code{xlab = NULL}, a label
is constructed from \code{time_unit}.}

\item{annotate}{logical. Should the follow-up and simulation notes be drawn
beneath the plot?}

\item{projection_col, simulation_col, milestone_col}{colours for the expected
projection, random trajectories, and milestone guides.}
}
\value{
Invisibly, a list containing the evaluated \code{design}, the
\code{projection} data frame, the \code{milestones} data frame, and the simulated
enrollment-time vectors in \code{simulations}.
}
\description{
Draws the expected cumulative enrollment curve for a
Goldilocks trial design, together with optional random enrollment
trajectories and projected interim and maximum-sample-size milestones.
}
\details{
The blue projection is
\eqn{1 + \Lambda(t)}, where \eqn{\Lambda(t)} is the cumulative intensity of
the piecewise-constant Poisson enrollment process. The first patient is
fixed at time zero, consistently with \code{\link[=enrollment]{enrollment()}}. A milestone's
projected time solves \eqn{1 + \Lambda(t) = N}. With a constant enrollment
rate this is also the mean arrival time, \eqn{(N - 1) / \lambda}. With a
piecewise rate it is an expected-count projection rather than the mean of
the corresponding arrival-time distribution.

If \code{x} supplies a stored design, explicitly supplied design arguments
override the corresponding stored values. This makes it possible, for
example, to compare a fitted design with a different enrollment rate.
}
\examples{
plot_enrollment(
  lambda = 20,
  N_total = 600,
  interim_look = 400,
  end_of_study = 12,
  n_sim = 20,
  seed = 20260727,
  time_unit = "months"
)

# Piecewise enrollment rates are supported.
plot_enrollment(
  lambda = c(8, 20),
  lambda_time = 6,
  N_total = 200,
  interim_look = c(100, 150),
  n_sim = 5,
  seed = 1,
  time_unit = "months"
)

}
